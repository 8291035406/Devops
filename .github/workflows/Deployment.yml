name: multiple deployment

on:
  push:
      branches:
        - main
jobs:
  Deployment:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        enviorment:
          - dev
          - qa
          - preprod
          - prepod2
          - prod
    steps:
      - name: Deployment to diffrent envs
        run: echo "Deploying to ${{matrix.enviorment}}"

    steps:
      - name: Tesing the hello word scriypt
        run: echo "welcomme to 1st job"
