name: Deplo using rolling update

on:
  pull_request: 
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest

    strategy:
      max-parallel: 2
      fail-fast: false
      matrix:
        server:
          - server1
          - server2
          - server3
          - server4
          - server5
          - server6
          - server7
          - server8
          - server9
          - server10

    steps:
      - name: Deplyment o diff sever
        run: |
          echo "deploy app to ${{ matrix.server}}"
  
